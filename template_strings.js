const myname = 'John';

const greeting = 'Hello, ' + myname + '!';

const greeting2 = `Hello, ${myname}!`;

const add = (a, b) => a + b;

const message = `Hello, ${myname}

How are you?

The sum of 2+3 is ${add(2, 3)}`;

console.log(message);
